$(document).ready(function() {
    init_loader();
});

/* --------------------------------------------
   Page loader
   --------------------------------------------- */
   function init_loader(){   
    $(".page-loader").delay(9500).slideUp("linear");
}
